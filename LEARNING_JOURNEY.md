# 🐶 狗狗养成项目 - 学习过程演变记录

> 从 0 到 1，一步一步的成长足迹

---

## 教程阶段：跟着教程走通基础（阶段一~三）

### 🎯 目标
跟着教程完成基础的狗狗展示和简单交互，跑通前后端

### 📋 完成内容
1. **技术选型**
   - 前端：Vue 3 + Vite
   - 后端：Node.js + Express
   - 数据库：MySQL

2. **基础页面**
   - 狗狗图片展示（使用 emoji）
   - 3 个状态条：饱食度、快乐度、亲密度
   - 2 个操作按钮：喂食、抚摸

3. **基础 API**
   - `GET /api/pet/stats` 获取状态
   - `POST /api/pet/feed` 喂食
   - `POST /api/pet/pet` 抚摸

4. **功能迭代**
   - 喂食时狗狗眨眼动画、对话气泡
   - 遛狗功能 + 3 种随机彩蛋结果
   - 心情系统（9 类状态共 27 条文案）
   - 取名功能

5. **难度优化**
   - 新增清洁度、精力值
   - 状态衰减（每 30 秒）
   - 冷却系统（喂食 10s、抚摸 5s、散步 30s、洗澡 60s）
   - 惩罚机制（过饱/精力不足时效果减半）

### 📌 教程阶段的主要收获
- **Vue3 Composition API**：`<script setup>` 语法、`ref`/`reactive`、计算属性
- **前后端联调**：Axios 请求、CORS 跨域、Vite 代理配置
- **状态管理思路**：从单一变量到多个响应式状态的组织
- **业务逻辑分层**：路由 → 控制器 → 数据库，理解 MVC 模式

### 📌 教程阶段遇到的问题
- MySQL 环境配置复杂，需要安装、配置账号
- 前后端跨域问题，需要配置 CORS
- 功能增加后代码变复杂，状态更新逻辑容易混乱

---

## 教程之外：自主扩展与升级（阶段四~七）

教程结束后，我发现项目还有很多可以提升的空间，于是自主规划并完成了下面这些扩展功能。这些已经超出了教程范围，是我自己查文档、设计、调试出来的。

### 阶段四：数据库迁移 - 解决成本问题

#### 🎯 目标
PlanetScale 改为收费 $5/月，想找一个完全免费的替代方案

#### 📋 完成内容
1. **技术调研**
   - 对比 MySQL、PostgreSQL、SQLite
   - 评估部署平台支持情况

2. **迁移决策**
   - 选择 SQLite (better-sqlite3)
   - 理由：零配置、嵌入式、文件级、完全免费

3. **代码迁移**
   - 移除 mysql2，安装 better-sqlite3
   - 重写数据库配置文件（连接池 → 同步 API）
   - 重写 SQL 语法（`?` 占位符、`datetime('now')` 等）
   - 保持 API 接口不变，前端无感知

4. **测试验证**
   - 6 个 API 接口全部通过
   - 前端页面正常显示

#### 📌 收获
- 理解了**关系型数据库的共性**：SQL 语法差异不大，但 API 调用方式差别很大
- 学会了**技术选型的权衡**：不一定要用"最流行"的，要选最适合场景的
- 掌握了**嵌入式数据库特性**：WAL 模式、事务、文件级持久化

---

### 阶段五：部署上线 - 让所有人都能访问

#### 🎯 目标
把项目部署到公网，让学姐和同学可以访问体验

#### 📋 完成内容
1. **部署平台调研与选型**
   - Render：要求绑卡（国内银行卡不支持），放弃
   - Cyclic.sh：国内无法访问，放弃
   - 最终方案：**Vercel（前端）+ Railway（后端）**

2. **配置部署文件**
   - `vercel.json`（前端 SPA 重写规则）
   - `railway.toml`（后端启动命令）
   - 根目录 `package.json`（让 Railway 识别 Node.js 项目）

3. **踩坑与修复**
   - Railway 识别不到 Node.js → 创建根目录 package.json
   - Vercel 构建失败 → 设置 Root Directory 为 `frontend`
   - esbuild install scripts 被阻止 → 添加 `.npmrc` 配置
   - package.json 含 BOM 字符 → 用 Write 工具重写
   - Railway 健康检查失败 → 监听 `process.env.PORT`

4. **最终成功**
   - 前端：https://dog-pet-app.vercel.app
   - 后端：https://dog-pet-app-production-8743.up.railway.app

#### 📌 收获
- **CI/CD 流程**：本地 push → GitHub → 自动触发部署，理解了现代部署的便利
- **环境变量管理**：开发/生产环境分离，敏感信息不入库
- **平台对比能力**：不是所有"免费"都真的免费（绑卡门槛、地域限制）
- **问题排查思路**：看构建日志 → 分析错误 → 搜索文档 → 修复 → 验证

---

### 阶段六：用户认证系统 - 让每个用户有自己的狗狗

#### 🎯 目标
教程里的项目是"单用户"的，所有人共享一只狗。我想让每个用户注册登录后，看到的是自己的狗狗。

#### 📋 完成内容
1. **后端**
   - 用 **bcryptjs** 加密密码（不能明文存！）
   - 用 **JWT (jsonwebtoken)** 签发登录令牌
   - 编写 `authMiddleware`：从请求头解析 token，验证用户身份
   - 给所有宠物 API 加上 `authenticate` 中间件
   - 所有数据库查询都带上 `WHERE user_id = ?`，实现**数据隔离**

2. **前端**
   - 新增 `/login` 和 `/register` 页面
   - 用 Vue Router 配置**路由守卫**：未登录用户访问 `/pet` 自动跳转到登录页
   - 用 localStorage 保存 token，刷新页面不丢失登录态
   - Axios 拦截器：每次请求自动附加 `Authorization: Bearer <token>`
   - 响应拦截器：收到 401 时自动清空登录态并跳转

3. **数据库迁移**
   - 新增 `users` 表（id, username, password_hash, created_at）
   - `pet_stats` 和 `pet_stats_history` 都加上 `user_id` 外键
   - 启动时自动迁移旧数据

#### 📌 收获
- **安全意识**：明白了密码必须加密、token 要有有效期、不能信任前端传来的 userId
- **JWT 工作原理**：无状态认证 vs Session 认证的区别
- **数据隔离设计**：所有查询都带 user_id，从数据库层面就避免越权
- **前端路由守卫**：登录态的优雅管理

---

### 阶段七：天气系统 - 让游戏更真实

#### 🎯 目标
让狗狗的状态变化和现实天气联动，晴天遛狗心情更好，雨天效果减半。

#### 📋 完成内容
1. **接入免费天气 API**
   - 调研了 OpenWeatherMap（要密钥）、高德（要密钥），最后选 **Open-Meteo**
   - 理由：完全免费、无需 API Key、覆盖全球

2. **后端天气服务**
   - 封装 `weatherService.js`：根据经纬度获取当前天气
   - WMO 天气代码映射（sunny/cloudy/rainy）
   - **30 分钟内存缓存**：减少 API 调用，提升响应速度
   - `getWeatherMultiplier()` 返回心情倍率：晴天 ×1.5、阴天 ×1.0、雨天 ×0.5

3. **天气影响逻辑**
   - 喂食、抚摸、散步、洗澡时的愉悦度增长都乘以天气倍率
   - 前端顶部显示天气图标、类型、温度

#### 📌 收获
- **第三方 API 集成**：HTTP 请求、JSON 解析、错误降级
- **缓存设计**：用内存 Map 做缓存，设置 TTL，理解了"缓存击穿"的简单预防
- **业务规则抽象**：把"天气影响心情"抽象成 `getWeatherMultiplier()`，业务代码只关心倍率
- **免费方案选型**：不花钱也能做出有用的功能

---

### 阶段八：狗狗日记 - 自动生成内容

#### 🎯 目标
每天根据狗狗的心情值、互动次数，自动生成一段有趣日记，像狗狗自己在说话。

#### 📋 完成内容
1. **数据库设计**
   - 新增 `pet_diary` 表（content, mood, feed_count, pet_count, walk_count, clean_count, weather_type）
   - UNIQUE(user_id, diary_date)：每天每用户只能有一篇日记
   - `pet_stats` 表加 `today_feed_count` 等每日计数字段

2. **日记生成服务**
   - `generateDiaryContent()`：根据状态生成狗狗口吻的文字
   - 多段拼接：天气开场 → 喂食感受 → 抚摸感受 → 散步感受 → 心情总结
   - 每个维度按次数分支（0次/几次/很多次），文案不同
   - 示例：「今天阳光明媚，旺财的一天开始啦！今天主人喂了我2次饭...」

3. **互动计数**
   - 每次喂食/抚摸/散步/洗澡后自动 +1
   - 日期变更时自动重置计数

4. **API 路由**
   - `GET /api/diary/today`：获取/自动生成今日日记
   - `POST /api/diary/generate`：强制重新生成
   - `GET /api/diary/list?page=1`：分页查询历史

5. **前端展示**
   - 可折叠的日记区域，显示日期、天气、心情标签
   - 「重新生成」按钮：刷新日记内容
   - 「查看历史」按钮 + 分页翻页功能

#### 📌 收获
- **内容生成思路**：不用 AI 也能做自动内容，模板 + 条件分支即可
- **分页查询**：`LIMIT/OFFSET` 的使用，理解了数据库分页原理
- **唯一约束**：UNIQUE 约束保证数据一致性，避免重复日记
- **前端交互设计**：折叠面板、分页器、乐观更新

---

### 阶段九：换装功能 - 视觉升级

#### 🎯 目标
给狗狗准备 3 套不同皮肤（默认款、戴帽子、穿披风），切换后保存到数据库。

#### 📋 完成内容
1. **3 套 SVG 皮肤**
   - 默认款：原始狗狗外观
   - 戴帽子：头顶棕色礼帽（含帽檐、帽身、帽带、金色装饰扣），带摇摆动画
   - 穿披风：红色超人披风（含内里、领口、黄色星星 logo），带飘动动画

2. **后端**
   - `pet_stats` 新增 `skin` 字段（默认 `'default'`）
   - `ALLOWED_SKINS` 白名单校验（防注入）
   - `POST /api/pet/skin`：切换皮肤并持久化

3. **前端**
   - 互动按钮下方新增换装区域，3 个并排按钮
   - 当前选中皮肤高亮显示
   - 乐观更新：点击立即切换，API 失败时自动回滚
   - 切换成功时弹出气泡「换了新造型，汪~」

#### 📌 收获
- **SVG 绘制**：手写 SVG 实现复杂图形（礼帽、披风、装饰扣）
- **CSS 动画**：`@keyframes` 实现摇摆、飘动效果
- **乐观更新**：先改 UI 再等响应，失败回滚，提升用户体验
- **白名单校验**：服务端不能信任前端传来的任意值

---

### 阶段十：线上问题排查实战 - 注册失败迷雾

#### 🎯 背景
部署上线后，发现注册功能一直失败，前后端排查了很久。

#### 📋 排查过程
1. **第一层假设（错误方向）**
   - 怀疑是 Vercel 没配置环境变量
   - 尝试引导用户去 Vercel 控制台配置 `VITE_API_BASE_URL`

2. **第二层假设（错误方向）**
   - 怀疑代码没推上去
   - 重新提交、推送、触发重新部署

3. **回归证据（找到真凶）**
   - 用 `curl` 直接请求后端 `/api/auth/register` → 返回 201 成功
   - 后端没问题！
   - 下载 Vercel 上的构建产物 `auth-*.js` 文件，**直接读源码**
   - 发现 baseURL 是 `...railway.app/api/pet/api/auth` —— **双重路径 bug！**

4. **根因定位**
   - 旧代码 `config.js` 导出 `${BACKEND}/api/pet`
   - `auth.js` 又 `import BASE_URL from './config.js'` 后拼 `/api/auth`
   - 结果变成 `/api/pet/api/auth`，404

5. **修复**
   - 三个 API 文件各自独立硬编码 `BACKEND`，不再相互依赖

6. **二次踩坑**
   - 修复后 Vercel "Redeploy" 没拉新代码（用了构建缓存）
   - 必须推新 commit 触发自动部署才行

#### 📌 收获（最重要的一课）
- **不要靠猜，要拿证据**：每次假设都要有 HTTP 响应、日志、源码支撑
- **构建缓存会骗人**：Vercel Redeploy 默认用缓存，必须推新 commit 或清缓存
- **模块依赖要小心**：A 文件导出带路径的 baseURL，B 文件 import 后又拼路径，很容易双重拼接
- **分层定位法**：前端 → 网络 → 后端 → 数据库，一层一层排除
- **坚持的力量**：这个问题排查了 4-5 轮才解决，但没有放弃，最终找到真凶

---

## 📊 成长收获总览

| 阶段 | 来源 | 主要收获 |
|------|------|----------|
| 阶段一~三 | 跟着教程 | Vue3 + Express 基础，前后端联调，状态管理 |
| 阶段四 | 自主扩展 | 数据库迁移，SQLite 特性，技术选型权衡 |
| 阶段五 | 自主扩展 | CI/CD，部署平台对比，问题排查 |
| 阶段六 | 自主扩展 | JWT 认证，数据隔离，安全意识 |
| 阶段七 | 自主扩展 | 第三方 API 集成，缓存设计，免费方案选型 |
| 阶段八 | 自主扩展 | 内容生成，分页查询，数据库约束 |
| 阶段九 | 自主扩展 | SVG 绘制，CSS 动画，乐观更新 |
| 阶段十 | 实战踩坑 | 系统性问题排查，证据驱动思维 |

---

## 🎯 教程之外，我额外做的事

教程只教了基础的 CRUD 和状态管理，但这些功能都是我自己设计实现的：

| 功能 | 教程有没有 | 难度 | 我的思考 |
|------|-----------|------|---------|
| 数据库迁移到 SQLite | ❌ 没有 | 中 | 主动解决收费问题 |
| Vercel + Railway 部署 | ❌ 没有 | 高 | 主动让项目能被别人访问 |
| 用户注册登录 | ❌ 没有 | 高 | 主动让每个用户有自己的狗 |
| JWT 认证 + 数据隔离 | ❌ 没有 | 高 | 主动学习安全知识 |
| 接入天气 API | ❌ 没有 | 中 | 主动让游戏更真实 |
| 天气影响心情 | ❌ 没有 | 中 | 主动设计业务规则 |
| 自动生成日记 | ❌ 没有 | 高 | 主动做有趣的内容系统 |
| 分页查询历史 | ❌ 没有 | 中 | 主动学习分页原理 |
| 换装皮肤系统 | ❌ 没有 | 中 | 主动做视觉升级 |
| SVG 手绘 + CSS 动画 | ❌ 没有 | 中 | 主动提升前端能力 |
| 线上问题排查 | ❌ 没有 | 高 | 主动面对真实工程问题 |

---

## 💡 核心理念

> 每一个复杂的系统，都是由简单的步骤叠加而成的。
> 
> **从 0 到 1 的关键**：开始 → 迭代 → 坚持 → 复盘
> 
> **遇到问题的态度**：不是阻碍，而是成长的机会！
> 
> **教程之外**：跟着教程走能学会基础，但真正的成长来自于自己发现问题、设计方案、解决问题。
