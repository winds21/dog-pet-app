# 🃏 项目知识速览卡片

> 狗狗养成项目 · 本次学习所有新知识点速查
> 用途：快速回忆「这个东西干嘛用的？我在项目里怎么用的？」

---

## 📑 目录

| 分类 | 卡片 |
|------|------|
| 🔐 认证与安全 | JWT、bcryptjs、数据隔离、路由守卫 |
| 🌤️ 第三方 API 集成 | Open-Meteo、HTTP 请求封装、内存缓存 |
| 📖 数据库与数据设计 | SQLite 迁移、UNIQUE 约束、分页查询、每日计数 |
| 🎨 前端进阶 | SVG 绘制、CSS 动画、乐观更新、Axios 拦截器 |
| 📦 部署与运维 | Vercel、Railway、SQLite vs MySQL、构建缓存 |
| 🕵️ 问题排查方法 | 证据驱动排查、双重路径 bug 复盘 |
| 🧠 AI 协作方法论 | 4 个核心原则、Prompt 技巧、角色分工 |

---

## 🔐 认证与安全

### 🟢 卡片 1：JWT (jsonwebtoken)

| 项 | 内容 |
|----|------|
| **是什么** | 无状态身份认证令牌，三部分组成（Header.Payload.Signature） |
| **用来干嘛** | 用户登录后，签发一个 token 给前端，后续请求带上这个 token 证明身份 |
| **本项目用法** | 注册/登录成功 → 签发 JWT（7 天有效期）→ 前端存 localStorage → 每次请求带 `Authorization: Bearer <token>` → 后端中间件解析验证 |
| **核心代码** | `jsonwebtoken.sign({ userId }, JWT_SECRET, { expiresIn: '7d' })` |
| **速记技巧** | 不用 Session 不用数据库存，**后端无状态**，token 本身就带用户信息 |
| **⚠️ 注意** | JWT_SECRET 要放在环境变量里，不能硬编码；token 被盗用很危险 |

---

### 🟢 卡片 2：bcryptjs 密码加密

| 项 | 内容 |
|----|------|
| **是什么** | 加盐哈希算法，专门用来存密码的 |
| **用来干嘛** | 数据库里绝对不能存明文密码！万一数据库泄露了密码全裸奔 |
| **本项目用法** | 注册时：`bcrypt.hash(password, 10)` 加盐 10 轮 → 存 hash；登录时：`bcrypt.compare(password, user.password_hash)` 比对 |
| **速记技巧** | 「哈希」是单向的，**永远无法从 hash 反推原密码**；每次 hash 结果还不一样（因为有随机盐） |
| **⚠️ 注意** | rounds 选 10-12 之间，太高太慢、太低不安全 |

---

### 🟢 卡片 3：数据隔离（用户级）

| 项 | 内容 |
|----|------|
| **是什么** | 每个用户只能看到/操作自己的数据，看不到别人的 |
| **用来干嘛** | 多用户系统的基础要求，否则张三能改李四的狗狗 |
| **本项目用法** | ① 所有表加 `user_id` 外键；② 中间件把 userId 放到 `req.user`；③ 所有 SQL 查询都加 `WHERE user_id = ?` |
| **速记技巧** | 永远不要相信前端传来的 `userId`，**一定从 token 里解析出来的用** |

---

### 🟢 卡片 4：路由守卫（Vue Router）

| 项 | 内容 |
|----|------|
| **是什么** | 路由跳转前的一个「检查站」，可以判断能不能进 |
| **用来干嘛** | 未登录用户访问受保护页面（如主页），自动跳去登录页 |
| **本项目用法** | `router.beforeEach((to, from, next) => { if (to.meta.requiresAuth && !token) next('/login') })` |
| **速记技巧** | 就是路由的「门禁保安」，没带通行证不让进 |

---

## 🌤️ 第三方 API 集成

### 🟢 卡片 5：Open-Meteo 天气 API

| 项 | 内容 |
|----|------|
| **是什么** | 完全免费、无需 API Key 的全球天气服务 |
| **用来干嘛** | 获取实时天气，影响狗狗心情（晴天效果好、雨天打折扣） |
| **本项目用法** | `fetch('https://api.open-meteo.com/v1/forecast?latitude=31.2304&longitude=121.4737&current_weather=true')`，上海坐标 |
| **速记技巧** | 找免费 API 三要素：**不要 Key、不要绑卡、国内能访问** → 这种最省心 |
| **参考替代** | 高德天气（国内准，要 Key）、OpenWeatherMap（要 Key） |

---

### 🟢 卡片 6：HTTP 请求封装（后端侧）

| 项 | 内容 |
|----|------|
| **是什么** | Node.js 里用 `fetch()` 调外部 HTTP API |
| **用来干嘛** | 后端主动去别的网站拉数据（和前端 Axios 一样，只不过是服务端发的） |
| **本项目用法** | 在 weatherService 里封装 fetch 调用，加上 try/catch 降级处理 |
| **速记技巧** | 第三方调用一定包 try/catch！外部服务挂了也不能让自己的服务一起挂，**优雅降级**（比如默认阴天倍率 1.0） |

---

### 🟢 卡片 7：内存缓存（Map + TTL）

| 项 | 内容 |
|----|------|
| **是什么** | 用内存里的 Map 存最近查过的数据，避免重复调 API |
| **用来干嘛** | 天气每 30 分钟才变，每次请求都去调 Open-Meteo 太浪费、太慢 |
| **本项目用法** | 存 `{ data, timestamp }`，取数据时判断 `Date.now() - timestamp < 30min`，过期再重新请求 |
| **速记技巧** | 缓存就是「最近查过的先存着，下次直接给旧的，别再跑一趟」 |
| **进阶方案** | 生产环境可以用 Redis，个人项目内存 Map 够用了 |

---

## 📖 数据库与数据设计

### 🟢 卡片 8：SQLite 数据库迁移（ALTER TABLE）

| 项 | 内容 |
|----|------|
| **是什么** | 已有表加新字段，旧用户数据不丢失 |
| **用来干嘛** | 本项目中途加了 skin、today_feed_count 等字段，老用户不能注册就报错 |
| **本项目用法** | 启动时 `PRAGMA table_info(pet_stats)` 查字段，不存在就 `ALTER TABLE pet_stats ADD COLUMN skin TEXT DEFAULT 'default'` |
| **速记技巧** | 「先检查，再补」，而不是每次 DROP 重建——**数据比代码重要一万倍** |

---

### 🟢 卡片 9：UNIQUE 唯一约束

| 项 | 内容 |
|----|------|
| **是什么** | 数据库层面保证某列（或组合）不能重复 |
| **用来干嘛** | pet_diary 里每天每用户只能一篇日记，防止重复插入 |
| **本项目用法** | `UNIQUE(user_id, diary_date)`，重复插入时 INSERT 会报错，在代码里捕获转 UPDATE |
| **速记技巧** | 业务规则里说「只能有一个」的，**在数据库层就锁死**，不要只靠前端/代码判断——靠不住 |

---

### 🟢 卡片 10：分页查询（LIMIT + OFFSET）

| 项 | 内容 |
|----|------|
| **是什么** | 一次只查一页数据，不是全表拉出来 |
| **用来干嘛** | 日记历史如果有几百篇，一次全取又慢又浪费 |
| **本项目用法** | `SELECT * FROM pet_diary ORDER BY diary_date DESC LIMIT ? OFFSET ?`，LIMIT = pageSize，OFFSET = (page - 1) * pageSize |
| **速记技巧** | 第 2 页每页 5 条 → 跳过前 5 条，取接下来 5 条 |
| **⚠️ 注意** | OFFSET 深分页会慢（10000 条以后），个人项目不用在意 |

---

### 🟢 卡片 11：每日计数字段 + 日期重置

| 项 | 内容 |
|----|------|
| **是什么** | 用 `today_feed_count` 等字段记录当天互动次数，跨天自动归零 |
| **用来干嘛** | 生成日记需要知道今天喂了几次，不统计就没数据 |
| **本项目用法** | 每次互动时：先判断今天是不是 `diary_date`，不是就归零 + 更新日期，然后计数 +1 |
| **速记技巧** | 「先校准日期，再操作计数」，否则凌晨跨天时计数会乱 |

---

## 🎨 前端进阶

### 🟢 卡片 12：SVG 自绘图形

| 项 | 内容 |
|----|------|
| **是什么** | 用 XML 标签写的矢量图，不依赖图片文件 |
| **用来干嘛** | 给狗狗画帽子、披风，不用找图片素材，体积小还能动画 |
| **本项目用法** | `<ellipse>` 画帽檐、`<rect>` 画帽身、`<polygon>` 画披风、`<animateTransform>` 做动画 |
| **速记技巧** | SVG 是「代码写的画」，**能直接嵌在 Vue 组件里**，用 v-if 控制显示/隐藏 |

---

### 🟢 卡片 13：CSS @keyframes 动画

| 项 | 内容 |
|----|------|
| **是什么** | CSS 原生动画，定义关键帧（0%、50%、100%）自动补中间帧 |
| **用来干嘛** | 帽子摇摆、披风飘动，让皮肤有动感 |
| **本项目用法** | `@keyframes sway { 0% { transform: rotate(-5deg) } 50% { rotate(5deg) } }`，加上 `animation: sway 3s ease-in-out infinite` |
| **速记技巧** | `infinite` 是无限循环，`ease-in-out` 是慢入慢出更自然 |

---

### 🟢 卡片 14：乐观更新

| 项 | 内容 |
|----|------|
| **是什么** | 先改前端 UI，再等后端响应；如果后端失败了就把 UI 改回去 |
| **用来干嘛** | 切换皮肤时不用转圈等 API，用户感觉更快 |
| **本项目用法** | 点击换皮肤 → 立刻把 `currentSkin` 改成新值 → 发 API → catch 里把 `currentSkin` 恢复旧值 + 提示失败 |
| **速记技巧** | 「先乐观地当成功，真失败了再道歉（回滚）」 |
| **⚠️ 注意** | 只适合不致命的操作（换皮肤、点赞），付钱这类操作绝对不能乐观更新 |

---

### 🟢 卡片 15：Axios 拦截器

| 项 | 内容 |
|----|------|
| **是什么** | 所有请求发出前/响应回来后的「统一检查站」 |
| **用来干嘛** | 请求拦截器：所有请求自动加 Authorization 头；响应拦截器：401 自动清登录态跳转 |
| **本项目用法** | `api.interceptors.request.use(config => { config.headers.Authorization = ...; return config })` |
| **速记技巧** | 不用每个 API 都手动加 token，**统一在拦截器里加**，省事还不容易漏 |

---

## 📦 部署与运维

### 🟢 卡片 16：Vercel（前端部署）

| 项 | 内容 |
|----|------|
| **是什么** | 前端专用部署平台，全球 CDN |
| **用来干嘛** | Vue/Vite/React 项目 push 代码自动构建部署 |
| **本项目用法** | 连 GitHub 仓库，`Root Directory` 设为 `frontend`，自动构建 `npm run build`，产物在 `dist` |
| **速记技巧** | Vercel 是前端的「一键托管」，不用买服务器不用配 Nginx |
| **⚠️ 注意** | SPA 路由刷新 404 要在 `vercel.json` 里写重写规则 |

---

### 🟢 卡片 17：Railway（后端部署）

| 项 | 内容 |
|----|------|
| **是什么** | 全能后端部署平台，支持 Node.js/Python/Go 等 |
| **用来干嘛** | 把 Express 服务跑起来，暴露公网 IP 和端口 |
| **本项目用法** | 连 GitHub，根目录放 package.json（让它识别 Node.js 项目），监听 `process.env.PORT`（不是固定 3000） |
| **速记技巧** | 后端部署一定要**监听环境变量给的 PORT**，不是自己写死端口 |
| **⚠️ 注意** | Railway 免费额度：每月 $5 额度 + 500 小时执行时间，够用但不无限 |

---

### 🟢 卡片 18：SQLite vs MySQL 选型

| 项 | SQLite | MySQL |
|----|--------|-------|
| **形态** | 一个文件，嵌入式 | 独立服务进程 |
| **安装配置** | 零，npm install 就有 | 要装服务、配账号、建库 |
| **并发** | 差（写锁是库级） | 好（行级锁） |
| **费用** | 完全免费 | PlanetScale 现在 $5/月 |
| **适合场景** | 个人项目、小工具、原型 | 正式产品、高并发 |
| **本项目选择** | ✅ 选 SQLite | ❌ 放弃 |

**速记结论**：个人小项目 → **SQLite 永远是第一选择**，省时间省精力。

---

### 🟢 卡片 19：构建缓存（Vercel/Railway）

| 项 | 内容 |
|----|------|
| **是什么** | 平台会缓存上次的构建产物，下次 redeploy 直接用旧的，不重新拉代码编译 |
| **用来干嘛** | 加快部署速度 |
| **踩过的坑** | 点了 Vercel 的「Redeploy」但代码没更新——因为用了缓存，旧 bug 还在！ |
| **解决方案** | ① 推新 commit 触发部署（推荐）；② Redeploy 时取消「Use existing build cache」勾选 |
| **速记技巧** | 「Redeploy 不是重新构建！」要重新从源码构建必须推新 commit 或清缓存 |

---

## 🕵️ 问题排查方法

### 🟢 卡片 20：证据驱动排查法（最重要！）

| 步骤 | 内容 | 本项目对应（注册失败案） |
|------|------|------------------------|
| ① 拿证据 | **不要猜**，先拿到真实的错误信息 | 先别想是不是环境变量，先拿 HTTP 响应状态码和内容 |
| ② 切层 | 分成 前端 → 网络 → 后端 → 数据库，逐层排除 | curl 直接请求后端 /register → 成功 → 后端没问题，问题在前端 |
| ③ 读源码 | 线上出问题就**下载线上的构建产物读** | 下载 Vercel 的 auth chunk → 看到 baseURL 是 `/api/pet/api/auth` → 找到真凶 |
| ④ 验证修复 | 修完要**端到端验证**，不只是本地跑通 | 本地构建 → 读 auth chunk 确认路径对 → 推送 → 等部署 → 再验证 |

**一句话口诀**：**不要猜！拿证据！读源码！**

---

## 🧠 AI 协作方法论

### 🟢 卡片 21：AI 协作的 4 个核心原则

| 原则 | 内容 | 反例（不要做） |
|------|------|--------------|
| 1️⃣ **我出主意，AI 出力气** | 功能设计、规则、取舍自己想，重复编码/查文档交给 AI | 「帮我做个养狗项目，随便做」→ 出来的东西肯定不想要 |
| 2️⃣ **不盲信，要判断** | AI 给的方案要自己想对不对，给的代码要自己过一遍 | AI 说「环境变量的问题」就去改，根本不验证——浪费时间 |
| 3️⃣ **证据 > 猜测** | 出了问题让 AI 去读日志/源码/响应，不要让它瞎猜 | 「为啥注册失败？」→ AI 会瞎猜 N 个方向，不如直接让它「读线上构建源码」 |
| 4️⃣ **Prompt 越具体越好** | 告诉它规则、约束、想要的格式，而不是模糊需求 | ❌「帮我写个日记功能」→ ✅「生成狗狗口吻日记，分 4 段，喂食 0/1-2/3-5/6+ 次各说不同的话，天气类型有 sunny/cloudy/rainy」 |

**一句话口诀**：你是**产品经理 + 架构师**，AI 是你的**高级程序员 + 文档助手**——分工不能搞反。

---

## 🎯 快速回忆表（最常用）

| 场景 | 第一反应 | 对应卡片 |
|------|---------|---------|
| 做用户登录 | 密码用 bcryptjs + 签发 JWT | #1, #2 |
| SQL 多用户查询 | 所有查询加 WHERE user_id = ? | #3 |
| 接外部 API | 包 try/catch + 加缓存 | #6, #7 |
| 加新数据库字段 | 启动时先查有没有，再 ALTER TABLE | #8 |
| 前端所有请求加 token | 用 Axios 请求拦截器 | #15 |
| 要让按钮点击有即时反馈 | 乐观更新（catch 里回滚） | #14 |
| 前端部署 | Vercel + 设置 Root Directory | #16 |
| 后端部署 | 监听 process.env.PORT | #17 |
| 个人项目选数据库 | 无脑选 SQLite | #18 |
| 部署了但代码没更新 | 推新 commit，不要只点 Redeploy | #19 |
| 出了问题找不出来 | 不要猜，拿 HTTP 响应/源码/日志 | #20 |
| 问 AI 之前想什么 | 我到底要什么？规则是什么？ | #21 |
